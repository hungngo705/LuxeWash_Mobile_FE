/**
 * UI Components barrel export
 *
 * Gom (re-export) toàn bộ component UI dùng chung về một chỗ để import gọn.
 */
export { Header } from './Header'; // Thanh tiêu đề màn hình
export { Card } from './Card'; // Thẻ nền trắng bo góc, có đổ bóng
export { PrimaryButton, SecondaryButton } from './Button'; // Nút chính/phụ
export { ProgressSteps } from './ProgressSteps'; // Thanh tiến trình nhiều bước
export { EmptyState } from './EmptyState'; // Trạng thái rỗng (không có dữ liệu)
export { GradientCard, GradientStrip } from './GradientCard'; // Thẻ/dải nền gradient
export { BottomActionBar } from './BottomActionBar'; // Thanh hành động cố định dưới màn hình
